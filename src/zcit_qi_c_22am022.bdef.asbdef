projection;
strict ( 2 );
use draft;

define behavior for ZCIT_QI_C_22AM022 alias QIHeader
{
  use create;
  use update;
  use delete;
  use action Edit;
  use action Activate;
  use action Discard;
  use action Prepare;
  use action Resume;
  use association _qiitem { create; with draft; }
}

define behavior for ZCIT_QI_OC_22AM022 alias QIItem
{
  use update;
  use delete;
  use association _qiheader { with draft; }
}